<h2><%= __('Advanced Loan Calculator') %></h2>
<p><%= __('Calculate your loan details including monthly payments, total interest, and loan term.') %></p>

<form id="loan-form">
  <label for="loan-amount"><%= __('Loan Amount:') %></label>
  <input type="number" id="loan-amount" name="loanAmount" step="0.01" required>

  <label for="interest-rate"><%= __('Interest Rate (% per annum):') %></label>
  <input type="number" id="interest-rate" name="interestRate" step="0.01" required>

  <label for="loan-term"><%= __('Loan Term (years):') %></label>
  <input type="number" id="loan-term" name="loanTerm" required>

  <label for="monthly-payment"><%= __('Desired Monthly Payment (leave blank if unknown):') %></label>
  <input type="number" id="monthly-payment" name="monthlyPayment" step="0.01">

  <button type="submit"><%= __('Calculate') %></button>
</form>

<p id="result"></p>

<script src="/assets/js/advancedLoanCalculator.js"></script>