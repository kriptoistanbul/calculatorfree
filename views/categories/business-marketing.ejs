<%
  title = __('Business and Marketing Calculators');
  description = __('Analyze business metrics using these calculators.');
%>

<section class="category-page">
  <h2><%= __('Business and Marketing Calculators') %></h2>
  <p><%= __('Optimize your business strategies with our calculators.') %></p>
  <div class="calculator-list">
    <div class="calculator-card">
      <h3><%= __('ROI Calculator') %></h3>
      <p><%= __('Calculate Return on Investment.') %></p>
      <a href="<%= langPrefix %>/calculators/roi" class="btn"><%= __('Calculate ROI') %></a>
    </div>
    <div class="calculator-card">
      <h3><%= __('Break-even Analysis Calculator') %></h3>
      <p><%= __('Calculate your break-even point.') %></p>
      <a href="<%= langPrefix %>/calculators/break-even" class="btn"><%= __('Calculate Break-even') %></a>
    </div>
    <div class="calculator-card">
      <h3><%= __('Project Cost Estimator') %></h3>
      <p><%= __('Estimate your project costs.') %></p>
      <a href="<%= langPrefix %>/calculators/project-cost" class="btn"><%= __('Estimate Project Cost') %></a>
    </div>
    <!-- Add more business and marketing calculators here as needed -->
  </div>
</section>